import { FeaturedImagesDirective } from './featured-images.directive';

describe('FeaturedImagesDirective', () => {
  it('should create an instance', () => {
    const directive = new FeaturedImagesDirective();
    expect(directive).toBeTruthy();
  });
});
