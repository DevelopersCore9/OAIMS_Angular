version: 1
frontend:
  phases:
    build:
      commands:
        - npm i npm@latest -g
        - npm run build
  artifacts:
    baseDirectory: dist/OAIMS
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
